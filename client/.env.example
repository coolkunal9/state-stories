# Copy to .env and restart `npm start` after any change.
# Backend must be running: from repo root run `npm start` or `npm run dev`

REACT_APP_API_URL=http://localhost:8000/api

# Production (Vercel/Netlify): set this to your deployed API, e.g.
# REACT_APP_API_URL=https://your-api.example.com/api
